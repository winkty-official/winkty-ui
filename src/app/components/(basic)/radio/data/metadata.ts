import { Metadata } from "next";

const RadioPageMetadata = {
  title: "Radio",
  description: "Radio buttons allow users to select one option from a set.",
  meta: {
    title: "Area Radio Group - UI Components",
    description:
      "A flexible and accessible radio group component with rich content support.",
  },
} as {
  title: string;
  description: string;
  meta: Metadata;
};


export default RadioPageMetadata;