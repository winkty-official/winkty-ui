import manual from "../../../../../../public/registry/area-radio-group.json";

export const packageInfo = {
  cli: {
    command: `${process.env.BASE_URI}/registry/area-radio-group.json`,
  },
  manual,
};
