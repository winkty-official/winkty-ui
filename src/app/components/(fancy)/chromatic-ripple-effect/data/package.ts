import globalConfig from "@/config";
import manual from "../../../../../../public/registry/chromatic-ripple-effect.json";

export const packageInfo = {
  cli: {
    command: `${globalConfig.BASE_URI}/registry/chromatic-ripple-effect.json`,
  },
  manual,
};
