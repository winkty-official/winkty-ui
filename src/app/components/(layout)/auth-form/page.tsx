import AuthFormDocs from "@/components/library/auth-form/docs";
import { AuthFormMetaData } from "@/components/library/auth-form/docs/metadata";

export const metadata = AuthFormMetaData;

export default function AuthFormPage() {
  return <AuthFormDocs />;
}
