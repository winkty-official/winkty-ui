import globalConfig from "@/config";
import manual from "../../../../../public/registry/input-field.json";

export const packageInfo = {
  cli: {
    command: `${globalConfig.BASE_URI}/registry/input-field.json`,
  },
  manual,
};
